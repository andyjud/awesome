<fade-in class="opacity-0 w-full" _="on load transition my opacity to 1 over 0.3 seconds">
    <div class="w-full lg:w-[50rem] mx-auto mb-20">
    
        {% if comments %}
    
        {% for comment in comments %}
        {% include 'a_posts/comment.html' %}
        {% endfor %}
    
        {% else %}
        <div class="w-full text-center mt-10">No likes on my comments just yet</div>
        {% endif %}
        
    </div>
</fade-in>
